{
  "name": "media-manager",
  "version": "0.1.0",
  "description": "Local photo/video manager (Electron)",
  "main": "main.js",
  "scripts": {
    "start": ".\\node_modules\\electron\\dist\\electron.exe --disable-gpu --disable-gpu-sandbox --disable-software-rasterizer ."
  },
  "dependencies": {
    "ffmpeg-static": "^5.1.0",
    "sharp": "^0.32.0"
  },
  "devDependencies": {
    "electron": "^26.0.0"
  }
}
